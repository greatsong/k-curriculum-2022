# 데이터 카드 (Data Card)

이 문서는 "무엇을 위해 만들었고 무엇에 쓰면 안 되는가"를 적습니다. 필드 정의는 [docs/SCHEMA.md](../docs/SCHEMA.md), 변경 이력은 [CHANGELOG.md](../CHANGELOG.md), 건수와 정책의 정본은 [manifest.json](./manifest.json)입니다.

## 개요

2022 개정 교육과정 성취기준과, 서로 다른 교과의 성취기준을 잇는 융합 연결. 융합(교과 통합) 수업 설계와 교육과정 지식 그래프 연구를 위한 데이터셋.

| 파일 | 항목 수 | 설명 |
|------|--------|------|
| `standards.json` | 6,444 | 성취기준 (2022 개정, 교육부 고시 원문 verbatim) |
| `links.published.json` | 3,916 | 게시 기준을 통과한 융합 연결 (`quality_score` ≥ 0.70) |
| `links.candidate.json` | 11,758 | AI가 제안했으나 미검토인 연결 |
| `manifest.json` | — | 스키마 버전(2), 출처 커밋, 건수, 게시 정책 |

## 의도된 용도

- 교사가 한 성취기준에서 출발해 다른 교과의 연결 상대와 그 근거·수업 아이디어를 찾는 일
- 교육과정을 그래프로 다루는 연구 — 교과 간 거리, 학교급 간 계열성, 융합 주제 분포
- 성취기준 검색·추천 기능을 만드는 에듀테크 개발

## 이렇게 쓰지 마세요

- **연결을 "교육부가 정한 관계"로 읽지 마세요.** 연결은 전부 AI가 판정한 것이고 근거는 `rationale`에 있습니다. 교육과정 문서에는 이런 연결이 없습니다.
- **`candidate`를 게시 연결처럼 쓰지 마세요.** 미검토이고 품질 편차가 큽니다. 0.7대 4,495건은 검토 후보로, 0.6 이하는 실험용으로만.
- **`code`로 조인하지 마세요.** 같은 코드가 두 과목에 쓰인 11건이 있어 식별자는 `key`입니다.
- **`keywords`를 의미 분석에 쓰지 마세요.** 본문 앞 어절을 단순 분리한 값입니다.

## 생성 방법

- **성취기준** — 교육부 고시 별책 PDF 19종을 별책별 파서로 추출하고, 완결성·문장 verbatim·학교급·교과 귀속·영역·해설·고려사항·형식의 8개 게이트를 전부 통과한 것만 정본으로 삼았습니다(2026-09). 수식의 사설 영역(PUA) 글리프는 원문 대조로 유니코드 표준 기호로 복원했습니다.
- **연결** — 2단계 파이프라인. ① 성취기준 본문 임베딩의 코사인 유사도로 후보쌍을 결정적으로 추출 → ② LLM(claude-sonnet-5)이 후보를 번호로만 참조해 교육적 타당성을 판정하고 품질 점수·유형·근거·융합 주제·수업 아이디어를 생성. 코드는 [`../algorithm/`](../algorithm), 명세는 [docs/NARRATIVE.md](../docs/NARRATIVE.md).
- **게시 정책** — `quality_score` ≥ 0.70만 published. 양쪽 모두 산업수요 전문교과인 연결은 서비스 대상(일반계 고등학교)에 맞지 않아 candidate에 둡니다. 재판정에서 떨어진 연결은 candidate에도 싣지 않습니다.

## 분포

**학년군** (`grade_group`)

| 학년군 | 수 |
|--------|-----|
| 고선택 | 2,860 |
| 기타 (산업수요 전문교과) | 1,993 |
| 중1-3 | 714 |
| 초5-6 | 280 |
| 고공통 | 266 |
| 초3-4 | 231 |
| 초1-2 | 100 |

**연결 커버리지** — published 연결이 하나 이상 붙은 성취기준 2,570개(40%). 초등 1-2학년군 29%, 3-4학년군 30%, 5-6학년군 42%, 중학교 54%, 고등 공통 54%, 고등 선택 58%, 전문교과 8%. 제2외국어 회화·독일어·러시아어 계열 11개 과목과 스포츠 문화는 연결 0건.

**연결 유형** — same_concept 957 · cross_subject 888 · application 840 · prerequisite 659 · extension 572. **품질** — 0.9 이상 323 · 0.8대 3,289 · 0.7대 304.

## 한계 (요약 — 전체는 [KNOWN_ISSUES.md](../KNOWN_ISSUES.md))

- 모든 연결은 AI 생성 — published에도 억지 연결이 남아 있을 수 있습니다. 무작위 표본과 유사도 최하위 꼬리를 사람이 읽어 확인했지만 전수는 아닙니다.
- 커버리지가 고르지 않습니다. 고등 선택과목에 두껍고, 초등 저학년과 제2외국어에 얇습니다.
- `explanation`은 원문에 해설이 있는 71%만 채워져 있습니다.
- 산업수요 전문교과(1,993개)는 `school_level`이 비어 있고 연결도 8%뿐입니다. 특성화고 맥락에서 쓰기에는 부족합니다.

## 무결성 검증

```bash
node scripts/validate.mjs
```

`key` 고유성, 링크의 참조 무결성(dangling)·정규화·점수 범위, `link_type` 유효성을 검사합니다. PR마다 GitHub Actions가 같은 검사와 manifest 건수 대조를 돌립니다.

## 라이선스

데이터 [CC BY 4.0](../LICENSE-DATA) (성취기준 원문 출처: 교육부 2022 개정 교육과정). 코드 [MIT](../LICENSE).
