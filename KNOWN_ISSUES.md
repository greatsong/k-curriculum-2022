# 알려진 문제 (Known Issues)

이 데이터셋은 **완벽하지 않습니다.** 신뢰를 위해 우리가 아는 한계를 먼저 공개합니다.
여기 적힌 문제를 발견하셔도 놀라지 마시고, 여기 없는 문제를 발견하시면 꼭 알려주세요.

## 데이터 품질

### 1. 성취기준 본문의 잔존 손상 (일부)
원본은 정부 고시 문서(HWP/xlsx)에서 파싱했습니다. 그 과정에서 일부 성취기준의
`content`·`explanation`·`application_notes`에 다음이 남아 있을 수 있습니다.
- 해설/적용사항이 본문(content)에 섞여 들어감 (explanation bleed)
- 개행 위치 손상, 문장 중간 절단
- 표·머리글·꼬리글 텍스트 혼입

대규모 복원을 거쳤으나 **전수 검증은 미완**입니다. 특히 `explanation` 필드가
`content`보다 검증이 덜 되어 있습니다.

### 2. 학년군 표기 (`grade_group`) — 정규화했으나 근본 원인은 미해결
공개본에서는 오염 표기를 표준 학년군으로 정규화했습니다.
- `초2` → `초1-2`, `초4` → `초3-4`, `초6` → `초5-6`, `중1~3` → `중1-3`

이는 **표기 통일**이며, 원본 파싱 파이프라인이 왜 이런 오염을 만들었는지에 대한
근본 수정은 아직입니다. 코드 접두사(예: `[4…]`=3~4학년군)와 `grade_group`이
어긋나는 항목이 남아 있다면 신고해 주세요.

### 3. 수학·과학 특수문자
수식·기호(√, ×, ⁿ, ∼ 등)는 유니코드로 복원했으나, 원본 HWP 수식 폰트의
사설영역(PUA) 글리프가 텍스트로 굳은 흔적이 일부 남아 있을 수 있습니다.

### 4. `school_level` 빈 값 다수
전문교과·고등 선택과목 등 **2,797개** 항목의 `school_level`이 비어 있습니다
(`grade_group`이 `기타`인 1,490개 포함). 코드로 학교급을 유추해야 할 수 있습니다.

### 5. 1~2학년군 데이터 부족
초등 1~2학년군은 **국어·수학 2개 교과 52개**만 존재합니다. 통합교과(바른생활·
슬기로운생활·즐거운생활)·안전한생활은 아직 포함되지 않았습니다.

## 링크(연결) 품질

### 6. AI 생성 연결의 본질적 한계
모든 연결은 **AI가 제안**한 것입니다. 사람이 최종 검토한 것은 `links.published.json`
(quality ≥ 0.70)이지만, 그중에도 **억지스럽거나 피상적인 연결**이 있을 수 있습니다.
"이건 아닌데" 싶은 연결을 보시면 [억지 연결 신고](../../issues/new/choose)로 알려주세요.

### 7. `links.candidate.json`은 미검토입니다
9,149개 candidate 연결은 **품질 편차가 큽니다.** 참고·실험용으로만 쓰시고,
그대로 서비스에 노출하지 마세요.

### 8. 커버리지 불균형
연결 밀도가 교과별로 고르지 않습니다. 데이터가 풍부한 교과(과학·수학 등)에
연결이 몰려 있고, 1~2학년군·일부 예체능·전문교과는 연결이 희소합니다.

## 범위 밖 (Not in scope)

- 성취기준 **원문 자체의 교육과정적 판단**(적절성 등)은 다루지 않습니다. 원문은
  교육부 고시를 따릅니다.
- 개별 교과서·평가 문항·수업 지도안은 포함하지 않습니다.

---

**이 목록은 살아있는 문서입니다.** 새로운 문제가 확인되면 계속 추가합니다.
기여로 해결된 항목은 체크 표시와 함께 남겨 이력을 보존합니다.
