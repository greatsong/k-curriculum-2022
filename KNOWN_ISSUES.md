# 알려진 문제 (Known Issues)

이 데이터셋은 **완벽하지 않습니다.** 신뢰를 위해 우리가 아는 한계를 먼저 공개합니다.
여기 적힌 문제를 발견하셔도 놀라지 마시고, 여기 없는 문제를 발견하시면 꼭 알려주세요.

## 데이터 품질

### 1. [✅ 해결 2026-09-05] 성취기준 본문의 잔존 손상
2026-09-05에 교육부 고시 별책 PDF 19종을 별책별 파서로 다시 추출하고, 완결성·문장 verbatim·
학교급·교과 귀속·영역·해설·고려사항·형식의 8개 게이트를 전부 통과한 결과로 정본을
교체했습니다. 해설 혼입(explanation bleed)·개행 손상·문장 절단·표 머리글 혼입은 `content`에서
0건이고, `explanation`은 코드별 해설을 원문에서 다시 붙였습니다. 원문 PDF 텍스트 추출 자체의
잡음(표 셀 줄바꿈, 문장 끝 마침표 누락)은 2차 판정으로 걸렀습니다.

남은 한계: 원문에 해설이 없는 29%는 `explanation`이 빈 값입니다. 원문 오탈자는 원문 그대로 둡니다.

### 2. [✅ 해결 2026-09-05] 학년군 표기 (`grade_group`)
정본을 다시 만들면서 `grade_group`·`school_level`을 코드 접두 규칙(`[2…]`=초1-2,
`[4…]`=초3-4, `[6…]`=초5-6, `[9…]`=중1-3, `[10…]`=고공통, `[12…]`=고선택, 한글 약어=전문교과)으로
일괄 생성하고 게이트에서 검사합니다. 오염 표기(`초2`, `중1~3`)는 원천에서 사라졌습니다.
코드 접두와 `grade_group`이 어긋나는 항목을 발견하면 신고해 주세요.

### 3. [✅ 해결 2026-09-05] 수학·과학 특수문자
원본 HWP 수식 폰트의 사설영역(PUA, U+E000–U+F8FF) 글리프는 원문 대조로 유니코드 표준
기호(√, ×, ⁿ, ∼ 등)로 복원했고, 현재 데이터에 PUA 글리프는 0건입니다(검증 스크립트가
아닌 별도 스캔으로 확인). 수식이 원문과 다르게 보이면 신고해 주세요.

### 4. `school_level` 빈 값 다수
산업수요 맞춤형 전문교과 **1,993개** 항목의 `school_level`이 비어 있습니다
(`grade_group`은 `기타`). 고등 선택과목은 2026-09 정본 재작성으로 전부 `고등학교`가 채워졌습니다.

### 5. 1~2학년군 연결 부족
초등 1~2학년군 성취기준은 국어·수학·통합교과(바른 생활·슬기로운 생활·즐거운 생활)
**100개**입니다(안전한 생활은 성취기준 형식이 아니라 미포함). 문제는 연결입니다 — 100개 중
published 연결이 붙은 것은 29개뿐입니다(2026-09-06 통합교과 8건 추가). 저학년 통합교과가 다른 교과와 어떻게 이어지는지는
별도 판정 기준이 필요합니다.

### 9. [✅ 해결 2026-07-23] 중학교 「사회」(지리·일반사회) 누락
초기 공개본에는 중학교 「사회」 과목의 **지리 영역·일반사회 영역(`[9사(지리)]`·`[9사(일사)]`)이
통째로 누락**되어 있었습니다. 원본 파싱 때 중학교 「역사」(`[9역]` 40개)를 `subject="사회"`로
잘못 담았고, 정작 「사회」(지리+일반사회)는 성취기준 목록이 **표(table) 안에 있어** 텍스트
추출기가 건너뛴 것이 원인입니다.

**조치**: 교육부 고시 제2022-33호 [별책 7] 사회과 교육과정 원문에서 표 셀을 재파싱하여
- `[9사(지리)]` 38개, `[9사(일사)]` 36개 = **74개 신규 추가**(`content` 전수 확보)
- 기존 `[9역]` 40개의 `subject`를 `사회` → **`역사`로 재라벨**

남은 한계: 새로 추가한 `[9사]` 항목은 `application_notes`(단원 단위 서술이라 미수집)가 비어 있고,
`explanation`은 일부만 채워졌으며, `keywords`는 본문 앞 어절을 단순 분리한 값입니다.

### 10. [✅ 해결 2026-07-23] 필수·선택 교과 추가 누락 (영어·통합교과·중학 선택)
9번과 같은 완결성 결함이 다른 교과에도 있었습니다. **영어**는 초3~중3(`[4영]`·`[6영]`·`[9영]`)이,
**초1-2 통합교과**(바른/슬기로운/즐거운 생활 `[2바]`·`[2슬]`·`[2즐]`)와 **중학 선택 교과**(환경
`[9환]`·보건 `[9보]`·진로와 직업 `[9진로]`)가 통째로 누락되어 있었습니다. 원인은 원본 파싱이
해당 별책을 넣지 않았거나(영어·통합교과), 성취기준 목록이 **표 안에 있어** 추출기가 건너뛴 것(별책18)입니다.

**조치**: 교육부 고시 제2022-33호 원문에서 verbatim 추출하여 **168개 신규 추가**
- 영어 61(별책14 PDF), 통합교과 48(별책2 PDF), 중학 선택 59(별책18 HWP 표 셀 재파싱)
- 2개 독립 감사팀이 원문 재추출 대조로 누락 0·verbatim 100% 확인
- 신규 성취기준용 융합 연결 **1,677개 생성**(quality≥0.8 → published 475)

남은 한계: 9번과 동일(`application_notes` 미수집, `explanation` 일부, `keywords` 단순 분리).
3D 레이아웃 좌표 재계산은 후속(신규 노드는 임베딩 좌표 폴백으로 렌더).

### 11. 같은 코드가 두 과목에 쓰인 성취기준 11건 — 고유 식별자는 `key`

교육부 코드 체계가 과목 간에 겹칩니다. `[12심독01-01]`~`[12심독02-04]` 8건은 「심화 영어 독해와 작문」과
「심화 독일어」에, `[12스문01-01]`~`[12스문01-03]` 3건은 「스포츠 문화」와 「스페인어권 문화」에 같은 코드로
쓰였습니다. 그래서 `code`만으로는 행을 특정할 수 없습니다.

- 모든 행에 `key` 필드가 있습니다. 겹치지 않는 5,433건은 `key === code`이고, 겹치는 11건만
  `"코드|과목"` 형식입니다(예: `"[12심독01-01]|심화 독일어"`).
- **링크의 `source_code`/`target_code`는 `key` 값입니다.** 성취기준과 조인할 때 `code`가 아니라 `key`로
  맞추세요. `scripts/validate.mjs`도 `key` 고유성과 `key` 참조로 검사합니다.
- `algorithm/generate-links.mjs`는 아직 `code` 기준이라, 이 11건은 각 쌍에서 하나만 다룹니다.

## 링크(연결) 품질

### 6. AI 생성 연결의 본질적 한계
모든 연결은 **AI가 제안**한 것입니다. 사람이 최종 검토한 것은 `links.published.json`
(quality ≥ 0.70)이지만, 그중에도 **억지스럽거나 피상적인 연결**이 있을 수 있습니다.
"이건 아닌데" 싶은 연결을 보시면 [억지 연결 신고](../../issues/new/choose)로 알려주세요.

### 7. `links.candidate.json`은 미검토입니다
11,758개 candidate 연결은 **품질 편차가 큽니다.** 참고·실험용으로만 쓰시고,
그대로 서비스에 노출하지 마세요.

### 8. 커버리지 불균형
연결 밀도가 교과별로 고르지 않습니다. 데이터가 풍부한 교과(과학·수학 등)에
연결이 몰려 있고, 1~2학년군·일부 예체능·전문교과는 연결이 희소합니다.

### 12. 전문교과끼리의 연결은 `links.published.json`에 싣지 않습니다

이 데이터셋을 만든 서비스의 대상은 **일반계 고등학교**입니다. 양쪽 모두 산업수요 맞춤형 전문교과
(`subject_group === "산업수요전문"`, 경영·금융 계열 41과목)인 연결은 일반고 교사가 쓸 일이 없어
2026-09-05부터 게시하지 않고 `links.candidate.json`에만 남깁니다(1,330건). 한쪽만 전문교과인 연결
(예: 「금융과 경제생활」↔「금융 일반」)은 일반고에서도 개설되는 과목이라 그대로 게시합니다.
특성화고 맥락에서 쓰려면 candidate 파일에서 `quality_score`로 걸러 쓰세요.

## 범위 밖 (Not in scope)

- 성취기준 **원문 자체의 교육과정적 판단**(적절성 등)은 다루지 않습니다. 원문은
  교육부 고시를 따릅니다.
- 개별 교과서·평가 문항·수업 지도안은 포함하지 않습니다.

---

**이 목록은 살아있는 문서입니다.** 새로운 문제가 확인되면 계속 추가합니다.
기여로 해결된 항목은 체크 표시와 함께 남겨 이력을 보존합니다.
